[project]
name = "laptop"
version = "0.1.0"
description = "Remote tab controller - receives REST commands and forwards to Pi via WebSocket"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]
